{
  "name": "smart-click-maps",
  "version": "1.0.0",
  "description": "Real-time viewer click clustering for Twitch streams",
  "private": true,
  "workspaces": [
    "server",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspace=server && npm run test --workspace=frontend",
    "deploy": "railway up"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
